class Solution:
    def flipAndInvertImage(self, image: List[List[int]]) -> List[List[int]]:
        ans=[]
        for i in image:
            i=i[::-1]
            invert=[]
            for j in i:
                invert.append(1-j)
            ans.append(invert)
        return ans
