class Solution:
    def nextGreatestLetter(self, letters: List[str], target: str) -> str:
        if target=="z":
            return letters[0]
        else:
            for i in range(len(letters)):
                if letters[i]>target:
                    return letters[i]
            return letters[0]
        