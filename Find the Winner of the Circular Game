class Solution:
    def findTheWinner(self, n: int, k: int) -> int:
        friend=[]
        for i in range(1,n+1):
            friend.append(i)
        j = 0
        while len(friend) > 1:
            j = (j + k - 1) % len(friend)  
            friend.pop(j)
        return friend[0]

        
