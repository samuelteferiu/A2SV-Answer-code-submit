class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        count=Counter(nums)
        target=len(nums)/2
        for key,value in count.items():
            if value>target:
                return key
        return -1
        
