class Solution:
    def selectionSort(self, arr):
        n = len(arr)
        for i in range(n):
            mins = i
            for j in range(i + 1, n):
                if arr[j] < arr[mins]:
                    mins = j
            arr[i], arr[mins] = arr[mins], arr[i]
        return arr
